# ── Base de datos ──────────────────────────────────────────────────────────────
# clinic_postgres es accesible por nombre de contenedor dentro de clinic_default
# La DATABASE_URL en docker-compose.yml sobrescribe esta, pero se mantiene aquí
# como referencia explícita
DATABASE_URL="postgresql://postgres:postgres123@clinic_postgres:5432/cigua_inv"

# ── JWT ────────────────────────────────────────────────────────────────────────
JWT_SECRET="9b9d6d68d6fc2c537472ef81fb96118cb995adf0c70ed0f8bc0365b6f6153b50"
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# ── Servidor ───────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# CORS — URL del frontend (para producción cambiar al dominio real)
FRONTEND_URL=http://localhost:8285

# ── ERP MSSQL ──────────────────────────────────────────────────────────────────
ERP_MSSQL_HOST=10.0.11.49
ERP_MSSQL_PORT=1433
ERP_MSSQL_USER=sa
ERP_MSSQL_PASSWORD=eli112190

# ── Logging ────────────────────────────────────────────────────────────────────
LOG_LEVEL=info
